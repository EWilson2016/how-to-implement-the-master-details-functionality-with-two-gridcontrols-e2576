<Window x:Class="WpfApplication15.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="MainWindow" xmlns:dxg="http://schemas.devexpress.com/winfx/2008/xaml/grid"  mc:Ignorable="d" xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
        xmlns:local="clr-namespace:WpfApplication15"
        Width="500" Height="500">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <dxg:GridControl Grid.Row="0" AutoPopulateColumns="True" Name="gridControl1" ItemsSource="{Binding Source1}">
            <dxg:GridControl.Columns>
            </dxg:GridControl.Columns>
            <dxg:GridControl.View>
                <dxg:TableView x:Name="view1" FocusedRow="{Binding CurrectRow1, Mode=TwoWay}"/>
            </dxg:GridControl.View>
        </dxg:GridControl>
        <dxg:GridControl Grid.Row="1"  AutoPopulateColumns="True" Name="gridControl2" ItemsSource="{Binding Source2}">
            <dxg:GridControl.Columns>
            </dxg:GridControl.Columns>
            <dxg:GridControl.View>
                <dxg:TableView />
            </dxg:GridControl.View>
        </dxg:GridControl>
    </Grid>
</Window>
